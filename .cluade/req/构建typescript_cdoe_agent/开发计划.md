# TXCode 开发计划

## 1. 开发原则

1. **先框架后模块** - 先搭建空框架，再填充各模块
2. **详细注释** - 所有代码必须有中文注释
3. **测试驱动** - 每个模块必须有测试用例覆盖

---

## 2. 开发阶段总览

```
阶段一：项目初始化      (Day 1)
阶段二：核心框架        (Day 2-3)
阶段三：数据库模块       (Day 4)
阶段四：配置模块        (Day 5)
阶段五：记忆模块        (Day 6-7)
阶段六：Session模块     (Day 8)
阶段七：Context模块    (Day 9)
阶段八：AI模块         (Day 10-14)
阶段九：Tools模块      (Day 15-16)
阶段十：Skill模块      (Day 17)
阶段十一：API模块      (Day 18-19)
阶段十二：CLI模块      (Day 20-22)
阶段十三：Web模块      (Day 23-25)
阶段十四：集成测试     (Day 26-28)
阶段十五：文档与发布   (Day 29-30)
```

---

## 3. 阶段一：项目初始化

### Day 1

#### 1.1 初始化项目
```bash
mkdir txcode && cd txcode
npm init -y
npm install typescript ts-node @types/node -D
npm install react ink @types/react -D
npm install express cors @types/express -D
npm install better-sqlite3 @types/better-sqlite3 -D
npm install uuid @types/uuid -D
npm install yaml js-yaml @types/js-yaml -D
npm install jest @types/jest ts-jest -D
```

#### 1.2 配置 TypeScript
```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "jsx": "react",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
}
```

#### 1.3 目录结构创建
```
src/
├── modules/
│   ├── db/
│   ├── memory/
│   ├── ai/
│   ├── session/
│   ├── context/
│   ├── skill/
│   ├── tools/
│   └── config/
├── api/
├── cli/
├── web/
├── components/
├── hooks/
└── shared/
tests/
```

#### 1.4 入口文件
```typescript
// src/index.ts
/**
 * TXCode - AI Coding Assistant
 * 
 * 主入口文件
 * - CLI 模式：使用 Ink 渲染 React 组件
 * - Web 模式：启动 Express 服务
 */

import { parseArgs } from './cli/args';

/**
 * 主函数
 * 根据命令行参数决定启动模式
 */
async function main() {
  const args = parseArgs(process.argv);
  
  if (args.command === 'web') {
    // 启动 Web 服务
    const { WebService } = await import('./web/web.service');
    const webService = new WebService(args.port);
    await webService.start();
  } else {
    // 启动 CLI
    const { render } = await import('ink');
    const React = await import('react');
    const { App } = await import('./components/App');
    render(React.createElement(App));
  }
}

main().catch(console.error);
```

#### 1.5 测试文件
```typescript
// tests/unit/project-init.test.ts

/**
 * 项目初始化测试
 * 
 * 测试内容：
 * 1. 目录结构是否正确
 * 2. 入口文件是否可执行
 * 3. 基础模块是否可导入
 */

describe('项目初始化', () => {
  test('应该能正确导入所有基础模块', () => {
    // 验证所有模块目录存在且可导入
    expect(() => {
      require('../../src/modules/db');
      require('../../src/modules/config');
    }).not.toThrow();
  });

  test('命令行参数解析应该正确', () => {
    const { parseArgs } = require('../../src/cli/args');
    
    // 测试各种参数组合
    expect(parseArgs(['node', 'txcode'])).toEqual({ command: 'chat' });
    expect(parseArgs(['node', 'txcode', 'web'])).toEqual({ command: 'web' });
    expect(parseArgs(['node', 'txcode', '--new'])).toEqual({ command: 'new' });
  });
});
```

---

## 4. 阶段二：核心框架

### Day 2-3

#### 2.1 DB 模块框架

**目标**: 创建 DB 模块的空壳，验证 SQLite 连接

**文件**:
- `src/modules/db/db.service.ts` - 数据库服务
- `src/modules/db/db.types.ts` - 类型定义
- `src/modules/db/index.ts` - 导出

```typescript
// src/modules/db/db.types.ts

/**
 * 数据库类型定义
 * 
 * DbService: 数据库服务类，负责数据库连接和初始化
 * Database: better-sqlite3 的数据库实例类型
 */

import Database from 'better-sqlite3';

export {
  /** 数据库服务类 */
  DbService,
  /** 数据库实例类型 */
  Database,
};
```

```typescript
// src/modules/db/db.service.ts

/**
 * 数据库服务
 * 
 * 职责：
 * - 管理 SQLite 数据库连接
 * - 执行数据库迁移
 * - 提供基础的 CRUD 操作
 * 
 * 使用方式：
 * ```typescript
 * import { dbService } from './db';
 * dbService.init();
 * const result = dbService.run('SELECT * FROM users');
 * ```
 */

import Database from 'better-sqlite3';
import * as path from 'path';
import * as fs from 'fs';

export class DbService {
  /** 数据库实例，延迟初始化 */
  private db: Database.Database | null = null;
  
  /** 数据库文件路径 */
  private dbPath: string;

  constructor(dbPath?: string) {
    // 默认路径：~/.txcode/data.db
    const home = process.env.HOME || process.env.USERPROFILE || '.';
    const txcodeDir = path.join(home, '.txcode');
    
    // 确保目录存在
    if (!fs.existsSync(txcodeDir)) {
      fs.mkdirSync(txcodeDir, { recursive: true });
    }

    this.dbPath = dbPath || path.join(txcodeDir, 'data.db');
  }

  /**
   * 初始化数据库
   * 
   * 执行操作：
   * 1. 创建数据库连接
   * 2. 设置 WAL 模式
   * 3. 启用外键约束
   * 4. 执行迁移
   */
  init(): void {
    this.db = new Database(this.dbPath);
    this.db.pragma('journal_mode = WAL');
    this.db.pragma('foreign_keys = ON');
    this.runMigrations();
  }

  /**
   * 获取数据库实例
   * 
   * @returns 数据库实例
   * @throws 如果未初始化则抛出错误
   */
  getDb(): Database.Database {
    if (!this.db) {
      this.init();
    }
    return this.db!;
  }

  /**
   * 执行 SQL 语句
   * 
   * @param sql - SQL 语句
   * @param params - 参数数组
   * @returns 执行结果
   */
  run(sql: string, params?: any): Database.RunResult {
    return this.getDb().prepare(sql).run(params);
  }

  /**
   * 查询单条记录
   * 
   * @param sql - SQL 语句
   * @param params - 参数数组
   * @returns 查询结果，未找到返回 undefined
   */
  get<T>(sql: string, params?: any): T | undefined {
    return this.getDb().prepare(sql).get(params) as T | undefined;
  }

  /**
   * 查询多条记录
   * 
   * @param sql - SQL 语句
   * @param params - 参数数组
   * @returns 查询结果数组
   */
  all<T>(sql: string, params?: any): T[] {
    return this.getDb().prepare(sql).all(params) as T[];
  }

  /**
   * 执行事务
   * 
   * @param fn - 事务函数
   * @returns 事务返回值
   */
  transaction<T>(fn: () => T): T {
    return this.getDb().transaction(fn)();
  }

  /**
   * 关闭数据库连接
   */
  close(): void {
    if (this.db) {
      this.db.close();
      this.db = null;
    }
  }

  /**
   * 执行数据库迁移
   * 
   * 迁移表结构：
   * - migrations: 记录已执行的迁移
   */
  private runMigrations(): void {
    if (!this.db) throw new Error('Database not initialized');

    // 创建迁移表（如果不存在）
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS migrations (
        id INTEGER PRIMARY KEY,
        name TEXT NOT NULL,
        applied_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )
    `);

    // 定义迁移列表
    const migrations = [
      this.migration001Initial,
    ];

    // 执行未应用的迁移
    for (let i = 0; i < migrations.length; i++) {
      const migration = migrations[i];
      const migrationId = i + 1;
      
      // 检查是否已应用
      const applied = this.db.prepare(
        'SELECT 1 FROM migrations WHERE id = ?'
      ).get(migrationId);

      if (!applied) {
        migration();
        this.db.prepare(
          'INSERT INTO migrations (id, name) VALUES (?, ?)'
        ).run(migrationId, migration.name);
      }
    }
  }

  /** 初始迁移：创建所有基础表 */
  private migration001Initial(): void {
    if (!this.db) return;
    
    // TODO: 创建所有表结构
    // 具体表结构在阶段三实现
  }
}

/** 单例导出 */
export const dbService = new DbService();
```

```typescript
// src/modules/db/index.ts

/**
 * DB 模块导出
 * 
 * 导出内容：
 * - DbService: 数据库服务类
 * - dbService: 数据库服务单例
 */

export { DbService, dbService } from './db.service';
export type { Database } from './db.types';
```

**测试文件**:
```typescript
// tests/unit/db.test.ts

/**
 * DB 模块单元测试
 * 
 * 测试内容：
 * 1. 数据库初始化
 * 2. CRUD 操作
 * 3. 事务执行
 */

import Database from 'better-sqlite3';
import * as fs from 'fs';
import * as path from 'path';
import { DbService } from '../../src/modules/db/db.service';

describe('DB 模块', () => {
  let dbService: DbService;
  const testDbPath = path.join(__dirname, 'test.db');

  beforeEach(() => {
    // 创建测试数据库实例
    dbService = new DbService(testDbPath);
    dbService.init();
  });

  afterEach(() => {
    // 清理
    dbService.close();
    if (fs.existsSync(testDbPath)) {
      fs.unlinkSync(testDbPath);
    }
  });

  describe('init()', () => {
    test('应该成功初始化数据库', () => {
      expect(dbService.getDb()).toBeInstanceOf(Database);
    });

    test('应该创建迁移表', () => {
      const result = dbService.get<{name: string}>(
        "SELECT name FROM sqlite_master WHERE type='table' AND name='migrations'"
      );
      expect(result).toBeDefined();
    });
  });

  describe('CRUD 操作', () => {
    test('run() 应该执行 INSERT 并返回结果', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      const result = dbService.run('INSERT INTO test (name) VALUES (?)', ['test']);
      
      expect(result.changes).toBe(1);
      expect(result.lastInsertRowid).toBe(1);
    });

    test('get() 应该返回单条记录', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      dbService.run('INSERT INTO test (name) VALUES (?)', ['test']);
      
      const result = dbService.get<{id: number; name: string}>(
        'SELECT * FROM test WHERE name = ?',
        ['test']
      );
      
      expect(result).toEqual({ id: 1, name: 'test' });
    });

    test('get() 查询不存在的记录应返回 undefined', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      
      const result = dbService.get('SELECT * FROM test WHERE id = ?', [999]);
      expect(result).toBeUndefined();
    });

    test('all() 应该返回多条记录', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      dbService.run('INSERT INTO test (name) VALUES (?)', ['a']);
      dbService.run('INSERT INTO test (name) VALUES (?)', ['b']);
      
      const results = dbService.all<{id: number; name: string}>('SELECT * FROM test');
      
      expect(results).toHaveLength(2);
      expect(results[0].name).toBe('a');
      expect(results[1].name).toBe('b');
    });
  });

  describe('transaction()', () => {
    test('应该正确执行事务', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      
      const result = dbService.transaction(() => {
        dbService.run('INSERT INTO test (name) VALUES (?)', ['t1']);
        dbService.run('INSERT INTO test (name) VALUES (?)', ['t2']);
        return 'success';
      });
      
      expect(result).toBe('success');
      
      const count = dbService.get<{count: number}>('SELECT COUNT(*) as count FROM test');
      expect(count?.count).toBe(2);
    });

    test('事务回滚应该生效', () => {
      dbService.run('CREATE TABLE test (id INTEGER PRIMARY KEY, name TEXT)');
      
      try {
        dbService.transaction(() => {
          dbService.run('INSERT INTO test (name) VALUES (?)', ['t1']);
          throw new Error('手动回滚');
        });
      } catch {
        // 预期内的错误
      }
      
      const count = dbService.get<{count: number}>('SELECT COUNT(*) as count FROM test');
      expect(count?.count).toBe(0);
    });
  });
});
```

---

### Day 3

#### 2.2 其他模块空壳

为每个模块创建空壳文件，包含：
- `index.ts` - 导出
- `*.types.ts` - 类型定义（仅有注释）
- `*.service.ts` - 服务类（仅有框架注释）

**模块列表**:
1. `modules/config`
2. `modules/memory`
3. `modules/session`
4. `modules/context`
5. `modules/ai`
6. `modules/skill`
7. `modules/tools`

```typescript
// src/modules/config/index.ts

/**
 * Config 模块
 * 
 * 职责：
 * - 管理 AI 服务商配置
 * - 管理模型配置
 * - 管理全局配置项
 */

export { ConfigService, configService } from './config.service';
```

```typescript
// src/modules/config/config.types.ts

/**
 * Config 模块类型定义
 * 
 * Provider: AI 服务商配置
 * Model: 模型配置
 */

export interface Provider {
  /** 服务商 ID */
  id: string;
  /** 服务商名称 */
  name: string;
  /** API Key */
  apiKey: string;
  /** API 基础 URL */
  baseUrl: string;
  /** 是否启用 */
  enabled: boolean;
  /** 是否为默认 */
  isDefault: boolean;
}

export interface Model {
  /** 模型 ID */
  id: string;
  /** 所属服务商 ID */
  providerId: string;
  /** 模型名称 */
  name: string;
  /** 是否启用 */
  enabled: boolean;
}
```

---

## 5. 阶段三：数据库模块 (Day 4)

#### 3.1 实现完整 Schema

```typescript
// src/modules/db/db.service.ts 中的迁移

private migration001Initial(): void {
  if (!this.db) return;
  
  // 会话表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS sessions (
      id TEXT PRIMARY KEY,
      title TEXT NOT NULL,
      project_path TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // 消息表（包含永久/临时记忆标记）
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS messages (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      session_id TEXT NOT NULL,
      role TEXT NOT NULL CHECK (role IN ('user', 'assistant', 'system', 'tool')),
      content TEXT NOT NULL,
      is_permanent INTEGER DEFAULT 0,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
    )
  `);

  // 项目知识表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS project_knowledge (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      project_path TEXT NOT NULL,
      key TEXT NOT NULL,
      value TEXT NOT NULL,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      UNIQUE(project_path, key)
    )
  `);

  // 代码片段表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS code_snippets (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      session_id TEXT,
      lang TEXT NOT NULL,
      description TEXT,
      code TEXT NOT NULL,
      tags TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE SET NULL
    )
  `);

  // 服务商表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS providers (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      api_key TEXT NOT NULL,
      base_url TEXT DEFAULT 'https://api.openai.com/v1',
      enabled INTEGER DEFAULT 1,
      is_default INTEGER DEFAULT 0,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // 模型表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS models (
      id TEXT PRIMARY KEY,
      provider_id TEXT NOT NULL,
      name TEXT NOT NULL,
      enabled INTEGER DEFAULT 1,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      FOREIGN KEY (provider_id) REFERENCES providers(id) ON DELETE CASCADE
    )
  `);

  // 配置表
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS config (
      key TEXT PRIMARY KEY,
      value TEXT NOT NULL,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);

  // 创建索引
  this.db.exec(`
    CREATE INDEX IF NOT EXISTS idx_messages_session_id ON messages(session_id);
    CREATE INDEX IF NOT EXISTS idx_messages_created_at ON messages(created_at);
    CREATE INDEX IF NOT EXISTS idx_messages_permanent ON messages(session_id, is_permanent);
    CREATE INDEX IF NOT EXISTS idx_project_knowledge_path ON project_knowledge(project_path);
    CREATE INDEX IF NOT EXISTS idx_code_snippets_session ON code_snippets(session_id);
    CREATE INDEX IF NOT EXISTS idx_models_provider ON models(provider_id);
  `);

  // 初始化默认配置
  this.db.run(`INSERT OR IGNORE INTO config (key, value) VALUES ('ai.maxToolIterations', '10')`);
  this.db.run(`INSERT OR IGNORE INTO config (key, value) VALUES ('ai.maxSessionCompression', '5')`);
  this.db.run(`INSERT OR IGNORE INTO config (key, value) VALUES ('web.port', '40000')`);
}
```

#### 3.2 测试

```typescript
// tests/unit/db-schema.test.ts

describe('数据库 Schema', () => {
  let dbService: DbService;

  beforeEach(() => {
    dbService = new DbService(':memory:');
    dbService.init();
  });

  afterEach(() => {
    dbService.close();
  });

  describe('sessions 表', () => {
    test('应该能够创建和查询会话', () => {
      dbService.run(
        'INSERT INTO sessions (id, title) VALUES (?, ?)',
        ['test-id', '测试会话']
      );
      
      const result = dbService.get<any>('SELECT * FROM sessions WHERE id = ?', ['test-id']);
      
      expect(result.title).toBe('测试会话');
    });
  });

  describe('messages 表', () => {
    test('应该支持 is_permanent 字段', () => {
      dbService.run('INSERT INTO sessions (id, title) VALUES (?, ?)', ['s1', 't']);
      
      // 永久消息
      dbService.run(
        'INSERT INTO messages (session_id, role, content, is_permanent) VALUES (?, ?, ?, ?)',
        ['s1', 'user', 'hello', 1]
      );
      
      // 临时消息
      dbService.run(
        'INSERT INTO messages (session_id, role, content, is_permanent) VALUES (?, ?, ?, ?)',
        ['s1', 'tool', 'tool result', 0]
      );
      
      const permanent = dbService.all<any>(
        'SELECT * FROM messages WHERE is_permanent = 1'
      );
      const temporary = dbService.all<any>(
        'SELECT * FROM messages WHERE is_permanent = 0'
      );
      
      expect(permanent).toHaveLength(1);
      expect(permanent[0].content).toBe('hello');
      expect(temporary).toHaveLength(1);
      expect(temporary[0].content).toBe('tool result');
    });
  });

  describe('config 表', () => {
    test('应该有默认配置项', () => {
      const maxIterations = dbService.get<{value: string}>(
        "SELECT value FROM config WHERE key = 'ai.maxToolIterations'"
      );
      const maxCompression = dbService.get<{value: string}>(
        "SELECT value FROM config WHERE key = 'ai.maxSessionCompression'"
      );
      
      expect(maxIterations?.value).toBe('10');
      expect(maxCompression?.value).toBe('5');
    });
  });
});
```

---

## 6. 阶段四：配置模块 (Day 5)

#### 4.1 Config Service 实现

```typescript
// src/modules/config/config.service.ts

/**
 * 配置服务
 * 
 * 职责：
 * - 提供配置的 CRUD 操作
 * - 管理 AI 服务商
 * - 管理模型
 * 
 * @example
 * ```typescript
 * const providers = await configService.getProviders();
 * await configService.addProvider({ id: 'openai', name: 'OpenAI', ... });
 * ```
 */

import { dbService } from '../db/db.service';
import { Provider, Model } from './config.types';

export class ConfigService {
  // ========== Provider 操作 ==========

  /**
   * 获取所有服务商
   * 
   * @returns 服务商列表
   */
  getProviders(): Provider[] {
    const rows = dbService.all<any>(
      'SELECT * FROM providers ORDER BY created_at'
    );
    
    return rows.map(this.rowToProvider);
  }

  /**
   * 获取单个服务商
   * 
   * @param id - 服务商 ID
   * @returns 服务商，不存在返回 undefined
   */
  getProvider(id: string): Provider | undefined {
    const row = dbService.get<any>(
      'SELECT * FROM providers WHERE id = ?',
      [id]
    );
    
    return row ? this.rowToProvider(row) : undefined;
  }

  /**
   * 获取默认服务商
   * 
   * @returns 默认服务商，不存在返回 undefined
   */
  getDefaultProvider(): Provider | undefined {
    const row = dbService.get<any>(
      'SELECT * FROM providers WHERE is_default = 1 LIMIT 1'
    );
    
    return row ? this.rowToProvider(row) : undefined;
  }

  /**
   * 添加服务商
   * 
   * @param provider - 服务商信息
   */
  addProvider(provider: Provider): void {
    // 如果是第一个，设为默认
    const count = dbService.get<{count: number}>(
      'SELECT COUNT(*) as count FROM providers'
    );
    const isFirst = (count?.count || 0) === 0;

    dbService.run(
      `INSERT INTO providers (id, name, api_key, base_url, enabled, is_default)
       VALUES (?, ?, ?, ?, ?, ?)`,
      [
        provider.id,
        provider.name,
        provider.apiKey,
        provider.baseUrl || 'https://api.openai.com/v1',
        provider.enabled ? 1 : 0,
        (isFirst || provider.isDefault) ? 1 : 0,
      ]
    );
  }

  /**
   * 更新服务商
   * 
   * @param id - 服务商 ID
   * @param data - 更新数据
   */
  updateProvider(id: string, data: Partial<Provider>): void {
    const updates: string[] = [];
    const values: any[] = [];

    if (data.name !== undefined) {
      updates.push('name = ?');
      values.push(data.name);
    }
    if (data.apiKey !== undefined) {
      updates.push('api_key = ?');
      values.push(data.apiKey);
    }
    if (data.baseUrl !== undefined) {
      updates.push('base_url = ?');
      values.push(data.baseUrl);
    }
    if (data.enabled !== undefined) {
      updates.push('enabled = ?');
      values.push(data.enabled ? 1 : 0);
    }
    if (data.isDefault !== undefined) {
      updates.push('is_default = ?');
      values.push(data.isDefault ? 1 : 0);
    }

    if (updates.length > 0) {
      updates.push('updated_at = CURRENT_TIMESTAMP');
      values.push(id);
      
      dbService.run(
        `UPDATE providers SET ${updates.join(', ')} WHERE id = ?`,
        values
      );
    }
  }

  /**
   * 删除服务商
   * 
   * @param id - 服务商 ID
   */
  deleteProvider(id: string): void {
    dbService.run('DELETE FROM providers WHERE id = ?', [id]);
  }

  /**
   * 设置默认服务商
   * 
   * @param id - 服务商 ID
   */
  setDefaultProvider(id: string): void {
    dbService.transaction(() => {
      // 清除所有默认
      dbService.run('UPDATE providers SET is_default = 0');
      // 设置新的默认
      dbService.run('UPDATE providers SET is_default = 1 WHERE id = ?', [id]);
    });
  }

  // ========== Model 操作 ==========

  /**
   * 获取服务商下的模型
   * 
   * @param providerId - 服务商 ID
   * @returns 模型列表
   */
  getModels(providerId: string): Model[] {
    const rows = dbService.all<any>(
      'SELECT * FROM models WHERE provider_id = ?',
      [providerId]
    );
    
    return rows.map(this.rowToModel);
  }

  /**
   * 获取所有模型
   * 
   * @returns 所有模型列表
   */
  getAllModels(): Model[] {
    const rows = dbService.all<any>('SELECT * FROM models');
    return rows.map(this.rowToModel);
  }

  /**
   * 添加模型
   * 
   * @param model - 模型信息
   */
  addModel(model: Model): void {
    dbService.run(
      'INSERT INTO models (id, provider_id, name, enabled) VALUES (?, ?, ?, ?)',
      [model.id, model.providerId, model.name, model.enabled ? 1 : 0]
    );
  }

  /**
   * 更新模型
   * 
   * @param id - 模型 ID
   * @param data - 更新数据
   */
  updateModel(id: string, data: Partial<Model>): void {
    const updates: string[] = [];
    const values: any[] = [];

    if (data.name !== undefined) {
      updates.push('name = ?');
      values.push(data.name);
    }
    if (data.enabled !== undefined) {
      updates.push('enabled = ?');
      values.push(data.enabled ? 1 : 0);
    }

    if (updates.length > 0) {
      updates.push('updated_at = CURRENT_TIMESTAMP');
      values.push(id);
      
      dbService.run(
        `UPDATE models SET ${updates.join(', ')} WHERE id = ?`,
        values
      );
    }
  }

  /**
   * 删除模型
   * 
   * @param id - 模型 ID
   */
  deleteModel(id: string): void {
    dbService.run('DELETE FROM models WHERE id = ?', [id]);
  }

  // ========== 配置项操作 ==========

  /**
   * 获取配置值
   * 
   * @param key - 配置 key
   * @returns 配置值，不存在返回 undefined
   */
  get<T = string>(key: string): T | undefined {
    const row = dbService.get<{value: string}>(
      'SELECT value FROM config WHERE key = ?',
      [key]
    );
    
    if (!row) return undefined;
    
    // 尝试解析为 JSON
    try {
      return JSON.parse(row.value) as T;
    } catch {
      return row.value as unknown as T;
    }
  }

  /**
   * 设置配置值
   * 
   * @param key - 配置 key
   * @param value - 配置值
   */
  set(key: string, value: any): void {
    const strValue = typeof value === 'string' ? value : JSON.stringify(value);
    
    dbService.run(
      `INSERT INTO config (key, value, updated_at) VALUES (?, ?, CURRENT_TIMESTAMP)
       ON CONFLICT(key) DO UPDATE SET value = excluded.value, updated_at = CURRENT_TIMESTAMP`,
      [key, strValue]
    );
  }

  // ========== 辅助方法 ==========

  private rowToProvider(row: any): Provider {
    return {
      id: row.id,
      name: row.name,
      apiKey: row.api_key,
      baseUrl: row.base_url,
      enabled: Boolean(row.enabled),
      isDefault: Boolean(row.is_default),
    };
  }

  private rowToModel(row: any): Model {
    return {
      id: row.id,
      providerId: row.provider_id,
      name: row.name,
      enabled: Boolean(row.enabled),
    };
  }
}

export const configService = new ConfigService();
```

#### 4.2 测试

```typescript
// tests/unit/config.test.ts

describe('Config 模块', () => {
  beforeEach(() => {
    dbService.init();
  });

  afterEach(() => {
    dbService.close();
  });

  describe('Provider 操作', () => {
    test('addProvider() 应该添加服务商', () => {
      configService.addProvider({
        id: 'test',
        name: 'Test Provider',
        apiKey: 'sk-test',
        baseUrl: 'https://api.test.com',
        enabled: true,
        isDefault: false,
      });

      const providers = configService.getProviders();
      expect(providers).toHaveLength(1);
      expect(providers[0].name).toBe('Test Provider');
    });

    test('第一个添加的服务商应该自动设为默认', () => {
      configService.addProvider({
        id: 'first',
        name: 'First',
        apiKey: 'sk-first',
        baseUrl: 'https://api.first.com',
        enabled: true,
        isDefault: false,
      });

      const provider = configService.getProvider('first');
      expect(provider?.isDefault).toBe(true);
    });

    test('deleteProvider() 应该删除服务商', () => {
      configService.addProvider({
        id: 'to-delete',
        name: 'Delete Me',
        apiKey: 'sk-delete',
        baseUrl: 'https://api.delete.com',
        enabled: true,
        isDefault: false,
      });

      configService.deleteProvider('to-delete');

      expect(configService.getProvider('to-delete')).toBeUndefined();
    });

    test('setDefaultProvider() 应该切换默认服务商', () => {
      configService.addProvider({
        id: 'p1',
        name: 'P1',
        apiKey: 'sk-p1',
        baseUrl: 'https://api.p1.com',
        enabled: true,
        isDefault: false,
      });

      configService.addProvider({
        id: 'p2',
        name: 'P2',
        apiKey: 'sk-p2',
        baseUrl: 'https://api.p2.com',
        enabled: true,
        isDefault: false,
      });

      configService.setDefaultProvider('p2');

      expect(configService.getProvider('p1')?.isDefault).toBe(false);
      expect(configService.getProvider('p2')?.isDefault).toBe(true);
    });
  });

  describe('配置项操作', () => {
    test('get() / set() 应该读写配置', () => {
      configService.set('test.key', 'test-value');
      
      const value = configService.get<string>('test.key');
      expect(value).toBe('test-value');
    });

    test('get() 应该支持 JSON 值', () => {
      const obj = { nested: { value: 123 } };
      configService.set('test.json', obj);
      
      const value = configService.get<typeof obj>('test.json');
      expect(value?.nested.value).toBe(123);
    });
  });
});
```

---

## 7. 阶段五：记忆模块 (Day 6-7)

#### 5.1 Memory Service 实现

核心功能：
- 消息管理（带 is_permanent）
- 永久/临时记忆分离
- 会话压缩

#### 5.2 测试覆盖

```typescript
// tests/unit/memory.test.ts

describe('Memory 模块', () => {
  beforeEach(() => {
    dbService.init();
    sessionService.create('test-session');
  });

  afterEach(() => {
    dbService.close();
  });

  describe('消息管理', () => {
    test('addMessage() 应该添加消息', () => {
      memoryService.addMessage('test-session', 'user', 'Hello', true);
      
      const messages = memoryService.getAllMessages('test-session');
      expect(messages).toHaveLength(1);
      expect(messages[0].content).toBe('Hello');
    });

    test('永久消息和临时消息应该正确分离', () => {
      memoryService.addMessage('test-session', 'user', 'user msg', true);
      memoryService.addMessage('test-session', 'tool', 'tool result', false);

      const permanent = memoryService.getPermanentMessages('test-session');
      expect(permanent).toHaveLength(1);
      expect(permanent[0].content).toBe('user msg');
    });
  });

  describe('会话压缩', () => {
    test('compressSession() 应该保留最近的永久消息', () => {
      // 添加 10 条永久消息
      for (let i = 0; i < 10; i++) {
        memoryService.addMessage('test-session', 'user', `msg ${i}`, true);
      }

      // 压缩到 5 条
      memoryService.compressSession('test-session', 5);

      const messages = memoryService.getPermanentMessages('test-session');
      expect(messages).toHaveLength(5);
      expect(messages[messages.length - 1].content).toBe('msg 9');
    });
  });
});
```

---

## 8. 阶段六：Session模块 (Day 8)

实现会话管理，测试 CRUD + 会话切换。

---

## 9. 阶段七：Context模块 (Day 9)

实现项目上下文获取，测试结构解析、Git 信息等。

---

## 10. 阶段八：AI模块 (Day 10-14)

**核心阶段**，包含：
- Day 10-11: OpenAI Provider
- Day 12-13: ReAct Agent + Parser
- Day 14: 记忆集成 + 测试

---

## 11. 阶段九：Tools模块 (Day 15-16)

实现 6 个内置工具，每个工具必须有单元测试。

---

## 12. 阶段十：Skill模块 (Day 17)

实现 Skill 加载和解析。

---

## 13. 阶段十一：API模块 (Day 18-19)

实现 Express 路由和 API 接口。

---

## 14. 阶段十二：CLI模块 (Day 20-22)

**Day 20-21**: React/Ink 组件
**Day 22**: 集成测试

---

## 15. 阶段十三：Web模块 (Day 23-25)

**Day 23-24**: Vue2 页面
**Day 25**: 集成测试

---

## 16. 阶段十四：集成测试 (Day 26-28)

```typescript
// tests/integration/full-flow.test.ts

/**
 * 完整流程集成测试
 * 
 * 测试场景：
 * 1. 创建会话
 * 2. 发送消息
 * 3. AI 处理（调用工具）
 * 4. 查看结果
 */

describe('完整流程', () => {
  test('应该能够完成一个简单的对话流程', async () => {
    // 1. 创建会话
    const session = sessionService.create('测试');
    expect(session).toBeDefined();

    // 2. 添加用户消息
    memoryService.addMessage(session.id, 'user', '你好', true);

    // 3. 获取上下文
    const context = contextService.getContext();

    // 4. 调用 AI
    const result = await aiService.chatWithReAct(
      '你好',
      { sessionId: session.id, projectPath: process.cwd() }
    );

    // 5. 验证结果
    expect(result.success).toBe(true);
    expect(result.answer).toBeDefined();
  });
});
```

---

## 17. 阶段十五：文档与发布 (Day 29-30)

- 完善 README
- 添加贡献指南
- 发布到 npm

---

## 18. 开发检查清单

每个模块完成时必须满足：

| 检查项 | 要求 |
|--------|------|
| 代码注释 | 所有函数/类必须有中文 JSDoc |
| 类型定义 | 所有公开接口必须有类型 |
| 单元测试 | 覆盖率 >= 80% |
| 编译通过 | `tsc` 无错误 |
| 测试通过 | `jest` 全部通过 |

---

## 19. 命令汇总

```bash
# 编译
npm run build

# 测试
npm test

# 测试覆盖率
npm run test:coverage

# 开发模式
npm run dev

# 发布
npm publish
```

---

请确认此开发计划是否符合预期，我可以开始实施。
